#pragma once

#include "azer/base/export.h"

namespace azer {
namespace views {
class FocusManager {
 public:
  FocusManager();
  ~FocusManager();
 private:
  DISALLOW_COPY_AND_ASSIGN(FocusManager);
};
}  // namespace views
}  // namespace azer
